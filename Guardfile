# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'haml', input: '.', output: '.', run_at_start: true do
  watch(/^.+(\.html\.haml)/)
end

guard 'sass', input: 'stylesheets', output: '.', style: 'compressed', run_at_start: true
